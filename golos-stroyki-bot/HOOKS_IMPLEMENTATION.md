# Реализация системы автоматических хуков (цепляющих фраз)

## Что реализовано

Добавлена система автоматической генерации коротких цепляющих фраз (хуков) для анкет специалистов и заявок заказчиков с использованием Deepseek AI.

## Файлы изменены

1. **bot.js** - основной файл бота
   - Добавлены функции `generateContractorHook()` и `generateOrderHook()` (строки 689-877)
   - Обновлены функции `finishForm()` и `finishOrderForm()` для генерации хуков
   - Обновлены функции `saveContractorToDatabase()` и `saveOrderToDatabase()` для сохранения хуков
   - Обновлены функции `formatContractorCard()` и `formatOrderCard()` для отображения хуков

2. **database-add-hooks.sql** - SQL миграция для базы данных

## Шаги по внедрению

### 1. Обновление базы данных

Выполните SQL миграцию в Supabase:

```sql
-- Откройте файл database-add-hooks.sql и выполните SQL запросы в SQL Editor в Supabase
```

Или выполните напрямую:

1. Откройте Supabase Dashboard
2. Перейдите в раздел SQL Editor
3. Выполните следующие команды:

```sql
-- Добавляем поле hook в таблицу contractors
ALTER TABLE contractors
ADD COLUMN hook TEXT;

COMMENT ON COLUMN contractors.hook IS 'Короткая цепляющая фраза (хук) для привлечения внимания, генерируется AI';

-- Добавляем поле hook в таблицу orders
ALTER TABLE orders
ADD COLUMN hook TEXT;

COMMENT ON COLUMN orders.hook IS 'Короткая цепляющая фраза (хук) для привлечения внимания, генерируется AI';
```

### 2. Проверка конфигурации

Убедитесь, что в файле `.env` настроен ключ Deepseek API:

```env
DEEPSEEK_API_KEY=sk-38769c90337e455eaf726dda1f18ad66
```

### 3. Перезапуск бота

```bash
# Остановите текущий процесс бота (Ctrl+C)
# Запустите бота заново
npm start
```

## Как это работает

### Генерация хуков для специалистов

При заполнении анкеты специалиста:

1. Пользователь заполняет все поля анкеты
2. При нажатии "Подтвердить" бот собирает данные:
   - Специализация
   - Опыт работы
   - Преимущества
   - Объекты работы
   - Формат работы
   - Готовность к командировкам

3. Отправляет запрос в Deepseek AI с промптом для создания хука
4. AI генерирует короткую цепляющую фразу (макс 60 символов)
5. Хук сохраняется в базу данных вместе с анкетой

### Генерация хуков для заявок

При создании заявки заказчиком:

1. Заказчик заполняет все поля заявки
2. При подтверждении бот собирает данные:
   - Тип запроса
   - Город
   - Тип объекта
   - Вид работ
   - Требования к исполнителю
   - Срок актуальности

3. Отправляет запрос в Deepseek AI
4. AI генерирует хук для заявки
5. Хук сохраняется в базу

### Отображение хуков

Хуки отображаются в карточках при поиске:

**С хуком:**
```
💡 Электрик с допуском СРО — работаем с промобъектами
━━━━━━━━━━━━━━━━━━━━━━━

📊 ИЩЕТ РАБОТУ
...
```

**Без хука (если не сгенерирован):**
```
📊 ИЩЕТ РАБОТУ
...
```

## Логика генерации

### Когда хук создается:

- Есть явные уникальные преимущества
- Опыт больше 1 года
- Указана конкретная специализация
- Есть особые условия работы

### Когда хук НЕ создается (AI возвращает null):

- Опыт меньше 1 года
- Нет уникальных преимуществ
- Стандартная анкета без особенностей
- Сгенерированный текст длиннее 60 символов

Это нормально - анкеты без хуков просто отображаются в обычном формате.

## Примеры хуков

### Для специалистов:

✅ "Электрик с допуском СРО — работаем с промобъектами"
✅ "Плиточник 10 лет — мозаика и сложные узоры"
✅ "Геодезист со своим оборудованием — без ожиданий"
✅ "Кровельщик — гарантия 5 лет на сложные кровли"

### Для заявок:

✅ "Отделка ЖК 200 квартир — долгосрочный контракт"
✅ "Кровля промобъекта — оплата каждую неделю"
✅ "Фасад 12 этажей — работа с вышкой и бригадой"
✅ "Электрика коттеджа — готовые проекты и материалы"

## Технические детали

### Функции генерации

- `generateContractorHook(contractorData)` - генерирует хук для специалиста
- `generateOrderHook(orderData)` - генерирует хук для заявки

### Параметры AI:

- Модель: `deepseek-chat`
- Temperature: `0.3` (для баланса креативности и точности)
- Max tokens: `80`

### Обработка ошибок:

- Если API недоступен - хук не создается (null)
- Если ключ не настроен - хук не создается (null)
- Если AI вернул "SKIP" - хук не создается (null)
- В любом случае анкета сохраняется успешно

## Проверка работы

После внедрения проверьте:

1. Создайте новую анкету специалиста с хорошими преимуществами
2. Проверьте в логах консоли сообщение "✅ Хук создан: ..."
3. Найдите анкету через поиск - хук должен отображаться вверху карточки
4. Проверьте в базе Supabase - поле `hook` должно быть заполнено

## Логи

В консоли будут появляться сообщения:

```
✅ Хук создан: Электрик с допуском СРО — работаем с промобъектами
```

или

```
⚠️ Хук не создан (SKIP или слишком длинный)
```

## Важно

- Хуки генерируются **только для новых анкет** (созданных после внедрения)
- Для старых анкет поле `hook` будет `null` - это нормально
- Генерация хука не является обязательной для успешного сохранения анкеты
- Хуки не показываются в канале (функция `publishToChannel` будет добавлена позже)

## Поддержка

Если возникают вопросы или проблемы, проверьте:

1. Выполнена ли SQL миграция в Supabase
2. Настроен ли `DEEPSEEK_API_KEY` в `.env`
3. Перезапущен ли бот после изменений
4. Нет ли ошибок в логах консоли
