# 📋 Руководство по интеграции с каналом сообщества

## ✅ Что было сделано

Интегрирована автоматическая публикация анкет и заявок в канал Telegram.

---

## 🔧 Шаги для активации

### 1. Выполнить SQL-миграцию

Запустите файл `migrations_channel_integration.sql` в Supabase:

```sql
-- Откройте Supabase → SQL Editor
-- Вставьте содержимое файла migrations_channel_integration.sql
-- Нажмите "Run"
```

Это добавит поле `channel_post_id` в таблицы `contractors` и `orders`.

---

### 2. Настроить переменные окружения в .env

Обновите файл `.env` (уже добавлено, нужно заполнить правильные значения):

```env
# Имя канала для ссылок (без @)
COMMUNITY_CHANNEL_NAME=GolosStroyki

# Username бота (без @, получить у @BotFather)
BOT_USERNAME=GolosStroyBot
```

**Как получить BOT_USERNAME:**
1. Открыть Telegram
2. Найти @BotFather
3. Отправить `/mybots`
4. Выбрать своего бота
5. Скопировать username (без @)

---

### 3. Настроить права бота в канале

#### Если бот УЖЕ администратор:

1. Открыть канал → Управление → Администраторы
2. Найти бота в списке
3. Проверить галочки:
   - ✅ Публикация сообщений
   - ✅ Редактирование сообщений
   - ✅ Удаление сообщений

#### Если бота НЕТ в администраторах:

1. Открыть канал → Управление → Администраторы
2. Нажать "Добавить администратора"
3. Найти бота (вставить @username)
4. Дать права:
   - ✅ Публикация сообщений
   - ✅ Редактирование сообщений
   - ✅ Удаление сообщений
5. Сохранить

---

### 4. Запустить бота

```bash
node bot.js
```

**Проверьте вывод в консоли:**

✅ Успешно:
```
🤖 Бот запущен: @GolosStroyBot (ID: 123456789)
✅ Supabase подключен успешно
✅ Канал подключен, публикация работает
```

⚠️ Нет прав:
```
⚠️ Бот администратор, но нет прав на публикацию
   Дайте боту право "Публикация сообщений" в настройках канала
```

❌ Не администратор:
```
❌ Бот не является администратором канала
   Добавьте бота в администраторы канала: @GolosStroyki
```

---

## 📊 Как это работает

### Для анкет специалистов:

1. Пользователь создаёт анкету через бота
2. Бот сохраняет анкету в БД
3. **Автоматически публикует в канал:**
   - 0 фото → только текст
   - 1 фото → фото + текст под ним
   - 2-6 фото → медиагруппа (красивая сетка)
4. Получает `message_id` от Telegram
5. Сохраняет в поле `channel_post_id`
6. Пользователь видит "Готово ✅"

### Формат поста в канале (БЕЗ контактов):

```
[Хук если есть]

Имя · Категория · [Роль]

Специализация: ...
Формат работы: ...
Город / регион: ...
Опыт: ...

Задачи / объекты:
...

Профессиональные преимущества: ...

Условия работы: ...

☎️ Контакты этого специалиста и другие предложения —
доступны в Базе сообщества
         ↑ ссылка на бота
```

### В боте (с контактами + ссылка на портфолио):

```
📊 ИЩЕТ РАБОТУ
━━━━━━━━━━━━━━━━━━━━━━━
Хук

Имя | Категория
🧠 [Роль]
━━━━━━━━━━━━━━━━━━━━━━━

🔧 Специализация: ...
💼 Формат работы: ...
📍 Город / регион: ...
⏱️ Опыт: ...
🏗 Задачи / объекты: ...
⭐️ Преимущества: ...
📋 Оформление: ...
🖼 Портфолио: смотреть в сообществе ← кликабельная ссылка
                ↑ ведёт на пост в канале

━━━━━━━━━━━━━━━━━━━━━━━
📞 +79123456789 | @username
```

**Важно:** Строка "Портфолио" показывается ТОЛЬКО если:
- Есть фото (`portfolio_photos.length > 0`)
- И пост опубликован (`channel_post_id != null`)

---

## 🔍 Что добавлено в код

### 1. Переменные окружения:
- `BOT_USERNAME` - имя бота для ссылок
- `COMMUNITY_CHANNEL_NAME` - имя канала для ссылок

### 2. Функции форматирования для канала:
- `formatChannelContractorPost()` - форматирует анкету БЕЗ контактов
- `formatChannelOrderPost()` - форматирует заявку БЕЗ контактов

### 3. Функции публикации:
- `publishContractorToChannel()` - публикует анкету в канал
- `publishOrderToChannel()` - публикует заявку в канал

### 4. Интеграция в процесс создания:
- `finishForm()` - добавлен вызов публикации анкеты
- `finishOrderForm()` - добавлен вызов публикации заявки

### 5. Ссылка на портфолио:
- `formatContractorCard()` - добавлена строка со ссылкой на пост в канале

### 6. Проверка прав при старте:
- Автоматическая проверка прав бота в канале
- Вывод статуса в консоль

---

## ❗ Важные моменты

### Обработка ошибок:
- Если публикация в канал не удалась → анкета всё равно сохраняется
- Пользователь видит "Готово ✅"
- Ошибка логируется в консоль
- Бот продолжает работать

### Логи в консоли:
```
📤 Публикация анкеты специалиста 44 в канал...
✅ Пост опубликован, message_id: 12345
✅ channel_post_id сохранён в базу для анкеты 44
```

При ошибке:
```
❌ Ошибка публикации анкеты в канал: Not enough rights to send photos in the chat
```

---

## 🧪 Тестирование

### Тест 1: Анкета без фото
1. Создать анкету без портфолио
2. Проверить: пост в канале (только текст)
3. Проверить: в Supabase есть `channel_post_id`
4. Проверить: в карточке НЕТ строки "Портфолио"

### Тест 2: Анкета с 1 фото
1. Создать анкету с 1 фото
2. Проверить: пост в канале (фото + текст)
3. Проверить: в карточке ЕСТЬ строка "Портфолио"
4. Кликнуть → открывается пост в канале

### Тест 3: Анкета с 5 фото
1. Создать анкету с 5 фото
2. Проверить: пост в канале (медиагруппа)
3. Проверить: ссылка "Портфолио" работает

### Тест 4: Заявка
1. Создать заявку
2. Проверить: пост в канале (только текст, без контактов)
3. Проверить: ссылка на бота работает

---

## 📁 Файлы изменений

- ✅ `.env` - добавлены BOT_USERNAME и COMMUNITY_CHANNEL_NAME
- ✅ `bot.js` - все изменения в коде
- ✅ `migrations_channel_integration.sql` - SQL-миграция для БД
- ✅ `CHANNEL_INTEGRATION_GUIDE.md` - это руководство

---

## 🆘 Решение проблем

### Проблема: "Бот не администратор канала"
**Решение:** Добавить бота в администраторы канала через настройки

### Проблема: "Нет прав на публикацию"
**Решение:** Включить право "Публикация сообщений" для бота

### Проблема: "Не удалось подключиться к каналу"
**Решение:** Проверить CHANNEL_ID в .env (должен быть @GolosStroyki)

### Проблема: "Портфолио не показывается в карточке"
**Причины:**
- Нет фото в анкете
- Или пост не опубликовался (проверить логи)
- Или channel_post_id = null в базе

---

## ✅ Готово!

Теперь при создании анкеты/заявки автоматически публикуется пост в канал без контактов, а в боте показывается полная карточка с контактами и ссылкой на портфолио.
