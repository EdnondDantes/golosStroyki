# Руководство по интеграции областей работ

## Что было сделано

Добавлена функциональность отображения **областей работ** вместо конкретных категорий в карточках специалистов и заявках.

### Пример изменения:

**Было:**
```
Албек | Сварщик трубопроводов
```

**Стало:**
```
Албек | Нефтегазовое и промышленное строительство
```

---

## Как это работает

### 1. Маппинг категорий на области работ

В файле `categories.js` добавлен маппинг `CATEGORY_TO_WORK_AREA`, который связывает каждую из 275+ категорий с соответствующей областью работ.

**Области работ для специалистов:**
- Дорожное строительство и инфраструктура
- Нефтегазовое и промышленное строительство
- Жилищное строительство
- Коммерческая и складская недвижимость
- Инженерные сети и ЖКХ
- Энергетическое строительство
- Спецстроительство: мосты, тоннели, метро
- Реконструкция и капитальный ремонт
- Премиум-интерьеры и fit-out
- Благоустройство и ландшафт
- Архитектура и градостроительство
- Инженерное проектирование
- Конструктив и расчёты

**Области работ для бригад:**
- Общестроительные работы
- Монолит, бетон и ЖБИ
- Отделочные работы
- Кровля и фасады
- Электромонтажные работы
- Сантехнические работы
- Дорожные и благоустроительные работы
- Специальные и высотные работы

**Области работ для компаний:**
- Подрядчики и специализированные компании
- Механизация и спецтехника
- Производство и материалы
- Инженерные и технические службы
- Управление строительством
- Проектирование и изыскания
- Снабжение и логистика
- Девелопмент и продажи
- Консалтинг и услуги

### 2. Автоматическое определение области работ

При создании анкеты/заявки:
1. AI определяет категорию (например, "Сварщик трубопроводов")
2. Функция `getWorkAreaByCategory()` находит соответствующую область работ
3. Область работ сохраняется в базу данных в поле `work_area`

### 3. Отображение в карточках

В карточках специалистов и заявках:
- **Вместо категории** показывается **область работ**
- Если область работ не определена, используется fallback на категорию
- Формат: `Имя | Область работ`

---

## Шаги для активации

### 1. Выполнить SQL-миграцию

Запустите файл `migrations_work_area.sql` в Supabase:

```sql
-- Откройте Supabase → SQL Editor
-- Вставьте содержимое файла migrations_work_area.sql
-- Нажмите "Run"
```

Это добавит поле `work_area` в таблицы `contractors` и `orders`.

### 2. Перезапустить бота

```bash
node bot.js
```

### 3. Проверить работу

Создайте новую анкету специалиста и проверьте, что в карточке отображается область работ вместо категории.

---

## Технические детали

### Изменённые файлы

1. **`categories.js`**
   - Добавлен маппинг `CATEGORY_TO_WORK_AREA`
   - Экспортируется вместе с `CATEGORIES`

2. **`bot.js`**
   - Добавлена функция `getWorkAreaByCategory(category)`
   - Обновлены функции сохранения: `saveContractorToDatabase()`, `saveOrderToDatabase()`
   - Обновлены функции форматирования: `formatContractorCard()`, `formatOrderCard()`
   - Обновлены функции форматирования для канала: `formatChannelContractorPost()`, `formatChannelOrderPost()`
   - При сохранении анкеты/заявки автоматически определяется и сохраняется область работ

3. **`migrations_work_area.sql`** (новый файл)
   - SQL-миграция для добавления поля `work_area`

### Схема данных

**Таблица `contractors`:**
```sql
work_area TEXT DEFAULT NULL -- Область работ специалиста
```

**Таблица `orders`:**
```sql
work_area TEXT DEFAULT NULL -- Область работ для заявки
```

### Логика fallback

```javascript
const displayWorkArea = contractor.work_area || contractor.category || contractor.specialization;
```

Если `work_area` не заполнено, используется:
1. Сначала `category` (старые данные)
2. Потом `specialization` (совсем старые данные)

---

## Примеры отображения

### Анкета специалиста

```
📊 ИЩЕТ РАБОТУ
━━━━━━━━━━━━━━━━━━━━━━━
Сварщик

Албек | Нефтегазовое и промышленное строительство
🧠 [заказчик]
━━━━━━━━━━━━━━━━━━━━━━━

🔧 Специализация: Сварщик
💼 Формат работы: Специалист
📍 Город / регион: Москва — готов к командировкам
⏱ Опыт: Менее 1 года
🏗 Задачи / объекты: Сваркой варю
📋 Оформление: Любой формат

━━━━━━━━━━━━━━━━━━━━━━━
📞 88005553535 | @username
```

### Заявка заказчика

```
📊 ИЩЮТ СОТРУДНИКА
━━━━━━━━━━━━━━━━━━━━━━━
Общестроительные работы

ООО "Стройка"
🏗️ [генподрядчик]
━━━━━━━━━━━━━━━━━━━━━━━

🔍 Ищут специалиста: Общестроительные работы
🏢 Заказчик: ООО "Стройка"
📍 Город / объект: Москва, ЖК "Новый"

📝 Задача: Кладка стен
━━━━━━━━━━━━━━━━━━━━━━━
📞 88005553535 | @username
```

---

## Обратная совместимость

Функциональность полностью обратно совместима:
- Старые анкеты без `work_area` будут показывать категорию
- Новые анкеты будут показывать область работ
- Никаких изменений в пользовательском интерфейсе не требуется

---

## Готово!

Теперь в карточках специалистов и заявках отображается **область работ** вместо конкретной категории, что делает поиск и фильтрацию более удобными.
