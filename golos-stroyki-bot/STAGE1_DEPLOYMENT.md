# Инструкция по деплою изменений Этапа 1

## Что было сделано

### 1. База данных
Создан SQL скрипт для новой таблицы `user_sources` в файле `database-user-sources.sql`

**Что нужно сделать:**
1. Открыть Supabase Dashboard
2. Перейти в раздел SQL Editor
3. Скопировать содержимое файла `database-user-sources.sql`
4. Выполнить SQL скрипт
5. Проверить что таблица создана успешно

### 2. Изменения в боте

#### Добавлено:
- **Стартовый экран приветствия** (`showWelcomeScreen`) - показывается при /start
- **Новая клавиатура** `welcomeScreenKeyboard` с двумя кнопками:
  - "👥 В сообщество «Голос Стройки»" - открывает ссылку на канал
  - "🗂 В Базу" - запускает проверку подписки и переход в базу
- **Функция сохранения источника** (`saveUserSource`) - записывает откуда пришел пользователь
- **Обработчик кнопки "В Базу"** (`go_to_database`) - проверяет подписку перед доступом к базе

#### Изменено:
- **Команда /start** теперь:
  1. Сохраняет источник трафика пользователя
  2. Показывает стартовый экран с выбором действия
  3. Проверка подписки перенесена на момент нажатия "В Базу"

## Новый флоу пользователя

```
/start
  ↓
Сохранение источника трафика в БД
  ↓
Стартовый экран с выбором:
  ├─ "В сообщество" → Открывается ссылка на канал
  └─ "В Базу" → Проверка подписки
                  ├─ Не подписан → Сообщение с просьбой подписаться
                  └─ Подписан → Главное меню
```

## Тестирование

После деплоя нужно протестировать:

1. **Новый пользователь:**
   - Отправить /start
   - Проверить что показывается стартовый экран
   - Проверить что запись в таблице `user_sources` создалась

2. **Кнопка "В сообщество":**
   - Нажать кнопку
   - Проверить что открывается канал

3. **Кнопка "В Базу" (не подписан):**
   - Нажать кнопку без подписки
   - Проверить что показывается сообщение с просьбой подписаться
   - Нажать "Я подписался" после подписки
   - Проверить что открывается главное меню

4. **Кнопка "В Базу" (подписан):**
   - Нажать кнопку будучи подписанным
   - Проверить что сразу открывается главное меню

5. **Повторный /start:**
   - Отправить /start повторно
   - Проверить что в БД не создается дубликат записи для пользователя

## Примечания

- В текущей реализации все пользователи сохраняются с источником "другое"
- В будущем можно добавить параметры к команде /start для отслеживания конкретного источника
- Например: `/start?source=instagram`, `/start?source=tiktok`, и т.д.
- Для этого нужно будет парсить параметры из `msg.text` в обработчике команды /start
