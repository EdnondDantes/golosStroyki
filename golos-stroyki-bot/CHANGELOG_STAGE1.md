# Changelog - Этап 1: Стартовый экран и отслеживание источников

## Дата: 2025-01-XX

### ✨ Новые возможности

#### 1. Стартовый экран приветствия
- При команде `/start` пользователь видит экран с выбором действия
- Две опции:
  - **"В сообщество"** - прямая ссылка на канал Telegram
  - **"В Базу"** - доступ к функционалу бота с проверкой подписки

#### 2. Отслеживание источников трафика
- Создана таблица `user_sources` в Supabase
- При первом `/start` сохраняется информация об источнике пользователя
- Защита от дубликатов - один пользователь = одна запись

### 🔧 Изменения в логике

#### До изменений:
```
/start → Проверка подписки → Главное меню
```

#### После изменений:
```
/start → Стартовый экран → "В Базу" → Проверка подписки → Главное меню
```

### 📦 Новые файлы

1. `database-user-sources.sql` - SQL скрипт для создания таблицы
2. `STAGE1_DEPLOYMENT.md` - инструкции по деплою
3. `CHANGELOG_STAGE1.md` - этот файл

### 🔨 Измененные функции

#### bot.js
- `bot.onText(/\/start/)` - добавлено сохранение источника и новый экран
- Новая функция `showWelcomeScreen()` - отображение стартового экрана
- Новая функция `saveUserSource()` - сохранение источника в БД
- Новый обработчик `go_to_database` в callback_query
- Новая клавиатура `welcomeScreenKeyboard`

### 🗄️ База данных

#### Новая таблица: user_sources
```sql
- id (BIGSERIAL PRIMARY KEY)
- telegram_id (BIGINT NOT NULL, UNIQUE)
- source (VARCHAR(50)) - инста, тикток, ютуб, статьи, контекст, другое
- created_at (TIMESTAMP)
```

### 🚀 Что дальше (следующие этапы)

- Этап 2-10: будут реализованы позже
- Возможность парсинга параметров из /start для точного отслеживания источника
- Аналитика по источникам трафика

### ⚠️ Важно

- Перед запуском бота нужно выполнить SQL скрипт в Supabase
- Протестировать новый флоу перед продакшеном
- Убедиться что переменные окружения настроены корректно
