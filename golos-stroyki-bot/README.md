# 🤖 Telegram-бот "Голос Стройки" - Каталог подрядчиков

## 📋 Описание проекта

Телеграм-бот для каталога строительных подрядчиков с функциями:
- ✅ Проверка подписки на канал
- 📝 Пошаговое заполнение анкеты подрядчика (8 шагов)
- 🎤 Голосовой ввод через Yandex SpeechKit
- 💾 Сохранение данных в Supabase
- 🎨 Красивое форматирование сообщений (жирный, курсив, эмодзи)

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Заполните все переменные в файле `.env` (инструкции ниже).

### 3. Настройка базы данных Supabase

См. раздел **"Настройка Supabase"** ниже.

### 4. Запуск бота

```bash
npm start
```

Для разработки с автоперезагрузкой:

```bash
npm run dev
```

---

## 🔧 Детальная настройка

### 1️⃣ Создание Telegram бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Голос Стройки Каталог")
   - Введите username бота (должен заканчиваться на `bot`, например: `golos_stroyki_bot`)
4. **Сохраните токен** - это ваш `BOT_TOKEN`
5. Настройте бота:
   ```
   /setdescription - Добавьте описание
   /setabouttext - Добавьте информацию "О боте"
   /setuserpic - Загрузите аватар
   ```

**Важно:** Добавьте вашего бота администратором в канал, чтобы он мог проверять подписки!

---

### 2️⃣ Настройка Supabase

#### Шаг 1: Создание проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Зарегистрируйтесь или войдите
3. Нажмите **"New Project"**
4. Заполните данные:
   - **Name**: golos-stroyki (или любое другое)
   - **Database Password**: придумайте надежный пароль
   - **Region**: выберите ближайший регион (например, Europe West)
5. Нажмите **"Create new project"**
6. Дождитесь создания проекта (~2 минуты)

#### Шаг 2: Получение ключей API

1. В боковом меню выберите **"Settings"** → **"API"**
2. Найдите и скопируйте:
   - **Project URL** → это ваш `SUPABASE_URL`
   - **anon/public key** → это ваш `SUPABASE_KEY`

#### Шаг 3: Создание таблицы для подрядчиков

1. В боковом меню выберите **"Table Editor"**
2. Нажмите **"Create a new table"**
3. Настройте таблицу:
   - **Name**: `contractors`
   - **Description**: Таблица анкет подрядчиков
   - Отключите опцию **"Enable Row Level Security (RLS)"** (для упрощения)

4. Добавьте следующие колонки:

| Column Name | Type | Default Value | Primary | Settings |
|-------------|------|---------------|---------|----------|
| id | int8 | auto | ✅ Yes | is identity, auto-increment |
| created_at | timestamptz | now() | ❌ No | nullable |
| telegram_id | int8 | - | ❌ No | not null |
| username | text | - | ❌ No | nullable |
| name | text | - | ❌ No | not null |
| city | text | - | ❌ No | not null |
| specialization | text | - | ❌ No | not null |
| experience | text | - | ❌ No | not null |
| description | text | - | ❌ No | not null |
| price | text | - | ❌ No | not null |
| portfolio_link | text | - | ❌ No | nullable |
| contact | text | - | ❌ No | not null |
| status | text | 'pending' | ❌ No | not null |

5. Нажмите **"Save"**

#### Шаг 4: SQL-запрос для создания таблицы (альтернативный способ)

Если хотите создать таблицу через SQL:

1. Перейдите в **"SQL Editor"**
2. Вставьте и выполните этот запрос:

```sql
-- Создание таблицы подрядчиков
CREATE TABLE contractors (
  id BIGSERIAL PRIMARY KEY,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  telegram_id BIGINT NOT NULL,
  username TEXT,
  name TEXT NOT NULL,
  city TEXT NOT NULL,
  specialization TEXT NOT NULL,
  experience TEXT NOT NULL,
  description TEXT NOT NULL,
  price TEXT NOT NULL,
  portfolio_link TEXT,
  contact TEXT NOT NULL,
  status TEXT DEFAULT 'pending' NOT NULL
);

-- Создание индекса для быстрого поиска по telegram_id
CREATE INDEX idx_contractors_telegram_id ON contractors(telegram_id);

-- Создание индекса для фильтрации по статусу
CREATE INDEX idx_contractors_status ON contractors(status);

-- Создание индекса для поиска по городу
CREATE INDEX idx_contractors_city ON contractors(city);
```

#### Шаг 5: Проверка таблицы

1. Перейдите в **"Table Editor"** → **"contractors"**
2. Убедитесь, что таблица создана и все колонки на месте

---

### 3️⃣ Настройка Yandex SpeechKit (для голосового ввода)

#### Шаг 1: Создание аккаунта Yandex Cloud

1. Перейдите на [cloud.yandex.ru](https://cloud.yandex.ru)
2. Зарегистрируйтесь или войдите
3. Активируйте пробный период (дается бонус на 1000+ рублей)

#### Шаг 2: Создание сервисного аккаунта

1. В консоли Yandex Cloud выберите ваш **Cloud**
2. Перейдите в **"Service accounts"**
3. Нажмите **"Create service account"**
4. Заполните:
   - **Name**: speechkit-bot
   - **Roles**: выберите `ai.speechkit-stt.user`
5. Нажмите **"Create"**

#### Шаг 3: Создание API ключа

1. Откройте созданный сервисный аккаунт
2. Перейдите на вкладку **"API keys"**
3. Нажмите **"Create API key"**
4. **Скопируйте и сохраните ключ** - это ваш `YANDEX_API_KEY`
   
   ⚠️ Ключ показывается только один раз!

#### Шаг 4: Получение Folder ID

1. В консоли Yandex Cloud откройте ваш **Folder**
2. В URL вы увидите: `cloud.yandex.ru/folders/b1g...`
3. Скопируйте ID после `/folders/` - это ваш `YANDEX_FOLDER_ID`

Или:

1. Перейдите в раздел **"Overview"** вашего folder
2. Скопируйте **"ID"** - это ваш `YANDEX_FOLDER_ID`

---

### 4️⃣ Настройка канала для проверки подписки

1. Создайте публичный Telegram канал (или используйте существующий)
2. Получите username канала (например: `@golos_stroyki`)
3. **Важно:** Добавьте вашего бота в администраторы канала:
   - Откройте настройки канала
   - Перейдите в **"Administrators"**
   - Нажмите **"Add Administrator"**
   - Найдите вашего бота и добавьте его
   - Можно оставить минимальные права
4. Используйте username канала как `CHANNEL_ID` (с символом `@`)

---

## 📝 Файл .env - итоговый пример

```env
# Telegram Bot
BOT_TOKEN=6123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567

# Supabase
SUPABASE_URL=https://abcdefghijklmnop.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSI...

# Yandex SpeechKit
YANDEX_API_KEY=AQVNabcDEF123ghIJKlmn456oPQRst789uvWXyz
YANDEX_FOLDER_ID=b1g1234567890abcdef

# Channel
CHANNEL_ID=@golos_stroyki
```

---

## 🎯 Структура проекта

```
golos-stroyki-bot/
├── bot.js              # Основной файл бота
├── package.json        # Зависимости проекта
├── .env                # Переменные окружения (не коммитить!)
├── .env.example        # Пример переменных
├── README.md           # Эта инструкция
└── node_modules/       # Установленные зависимости
```

---

## 🔍 Логика работы бота

### Пользовательский путь

1. **Старт бота** (`/start`)
   - Проверка подписки на канал
   - Если не подписан → просьба подписаться
   - Если подписан → главное меню

2. **Главное меню**
   - 🔍 Найти подрядчика (в разработке)
   - ➕ Добавить себя в каталог
   - ⭕️ Отправить жалобу
   - ❓ FAQ / Помощь
   - 💬 Сообщество

3. **Заполнение анкеты** (8 шагов)
   - Шаг 1: Имя/название компании
   - Шаг 2: Город
   - Шаг 3: Специализация
   - Шаг 4: Опыт работы
   - Шаг 5: Описание
   - Шаг 6: Цены
   - Шаг 7: Портфолио
   - Шаг 8: Контакты

4. **После заполнения**
   - Данные сохраняются в Supabase
   - Статус: `pending` (на модерации)
   - Пользователь получает уведомление

### Голосовой ввод

На каждом шаге анкеты пользователь может:
- Написать текстом
- Отправить голосовое сообщение 🎤

При получении голоса:
1. Бот скачивает файл
2. Отправляет в Yandex SpeechKit
3. Получает распознанный текст
4. Показывает пользователю, что распознано
5. Сохраняет ответ и переходит к следующему шагу

---

## 🛠 Дополнительные возможности

### Модерация анкет

Для модерации анкет можно создать отдельную админ-панель или использовать Supabase Dashboard:

1. Откройте **"Table Editor"** → **"contractors"**
2. Найдите анкеты со статусом `pending`
3. Измените статус на:
   - `approved` - одобрено
   - `rejected` - отклонено

### Уведомления пользователей

Можно добавить функцию отправки уведомлений:

```javascript
// Уведомление об одобрении
async function notifyApproval(telegramId) {
  await bot.sendMessage(telegramId, 
    '✅ Поздравляем! Твоя анкета одобрена и добавлена в каталог.');
}

// Уведомление об отклонении
async function notifyRejection(telegramId, reason) {
  await bot.sendMessage(telegramId, 
    `❌ К сожалению, твоя анкета не прошла модерацию.\n\nПричина: ${reason}`);
}
```

---

## 🐛 Устранение неполадок

### Бот не отвечает

1. Проверьте, что бот запущен: `npm start`
2. Проверьте токен в `.env`
3. Убедитесь, что бот не запущен в другом месте
4. Проверьте логи на наличие ошибок

### Ошибка подключения к Supabase

1. Проверьте `SUPABASE_URL` и `SUPABASE_KEY` в `.env`
2. Убедитесь, что проект Supabase активен
3. Проверьте, что таблица `contractors` создана

### Голосовой ввод не работает

1. Проверьте `YANDEX_API_KEY` и `YANDEX_FOLDER_ID`
2. Убедитесь, что баланс Yandex Cloud положительный
3. Проверьте права сервисного аккаунта

### Проверка подписки не работает

1. Убедитесь, что `CHANNEL_ID` указан с символом `@`
2. Проверьте, что бот добавлен в администраторы канала
3. Убедитесь, что канал публичный

---

## 📊 База данных - структура таблицы

```sql
contractors
├── id                  BIGSERIAL PRIMARY KEY
├── created_at          TIMESTAMPTZ (дата создания)
├── telegram_id         BIGINT (ID пользователя в Telegram)
├── username            TEXT (username в Telegram)
├── name                TEXT (имя/название компании)
├── city                TEXT (город работы)
├── specialization      TEXT (специализация)
├── experience          TEXT (опыт работы)
├── description         TEXT (описание)
├── price               TEXT (цены)
├── portfolio_link      TEXT (ссылка на портфолио)
├── contact             TEXT (контакт для связи)
└── status              TEXT (pending/approved/rejected)
```

---

## 🔐 Безопасность

1. **Никогда не коммитьте `.env` файл в Git!**
2. Добавьте `.env` в `.gitignore`:
   ```
   node_modules/
   .env
   ```
3. Храните токены в безопасности
4. Используйте RLS (Row Level Security) в Supabase для production
5. Регулярно обновляйте зависимости: `npm update`

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте эту документацию
2. Посмотрите логи бота на наличие ошибок
3. Проверьте документацию:
   - [Telegram Bot API](https://core.telegram.org/bots/api)
   - [Supabase Docs](https://supabase.com/docs)
   - [Yandex SpeechKit](https://cloud.yandex.ru/docs/speechkit/)

---

## 🚀 Следующие шаги

После запуска базового функционала можно добавить:

- [ ] Функцию поиска подрядчиков
- [ ] Фильтрацию по городу и специализации
- [ ] Систему рейтингов и отзывов
- [ ] Админ-панель для модерации
- [ ] Аналитику и статистику
- [ ] Платные функции (продвижение анкет)

---

**Успешного запуска! 🎉**
