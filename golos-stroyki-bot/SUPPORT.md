# 💬 Поддержка и помощь

## 🆘 Что-то не работает?

### Ошибка: "relation "user_roles" does not exist"

**Что это означает:** Таблица `user_roles` не была создана в Supabase.

**Как исправить:**
1. Откройте [Supabase Dashboard](https://app.supabase.com/)
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое `migrations_user_roles.sql`
4. Вставьте в SQL Editor
5. Нажмите **Run**
6. Перезагрузите бот

### Ошибка: Роль не сохраняется

**Возможные причины:**
1. Таблица `user_roles` не создана
2. SUPABASE_KEY не имеет прав на таблицу
3. Функция `saveUserRole()` не вызывается

**Как исправить:**
1. Проверьте логи бота в консоли
2. Убедитесь, что таблица `user_roles` создана (Table Editor)
3. Проверьте права доступа SUPABASE_KEY
4. Прочитайте [ROLE_IMPLEMENTATION_CHECKLIST.md](./ROLE_IMPLEMENTATION_CHECKLIST.md)

### Ошибка: Пользователь видит выбор роли при каждом входе

**Возможные причины:**
1. Таблица `user_roles` создана, но пуста
2. Функция `checkUserRole()` не работает
3. Роль не сохраняется корректно

**Как исправить:**
1. Проверьте в Supabase Table Editor → user_roles
2. Должна быть запись с вашим telegram_id
3. Если записи нет → роль не сохранилась
4. Проверьте логи бота при выборе роли

### Ошибка: Роль не попадает в contractors/orders

**Возможные причины:**
1. Функция `finishForm()` не получает роль из БД
2. Роль не была сохранена в `user_roles`

**Как исправить:**
1. Убедитесь, что роль сохраняется при выборе
2. Проверьте, что таблица `user_roles` имеет данные
3. Посмотрите на логи в консоли при создании анкеты

---

## 📞 Где искать помощь

### 1️⃣ Прочитайте документацию

| Вопрос | Документ |
|--------|----------|
| Как быстро внедрить? | [QUICKSTART.md](./QUICKSTART.md) |
| Пошаговая инструкция | [ROLE_IMPLEMENTATION_CHECKLIST.md](./ROLE_IMPLEMENTATION_CHECKLIST.md) |
| Подробная информация | [USER_ROLES_IMPLEMENTATION.md](./USER_ROLES_IMPLEMENTATION.md) |
| Архитектура системы | [ARCHITECTURE_DIAGRAM.md](./ARCHITECTURE_DIAGRAM.md) |
| Что изменилось? | [CHANGES_SUMMARY.md](./CHANGES_SUMMARY.md) |
| Общий индекс | [README_USER_ROLES.md](./README_USER_ROLES.md) |

### 2️⃣ Проверьте логи

**Логи бота:**
```bash
# В консоли должны быть сообщения типа:
✅ Пользователь 123456789 выбрал роль: подрядчик/компания
✅ Роль сохранена в БД
```

**Логи Supabase:**
1. Откройте Supabase Dashboard
2. Перейдите в **Logs**
3. Ищите ошибки при операциях с таблицей `user_roles`

### 3️⃣ Проверьте БД

```sql
-- Проверить, что таблица создана
SELECT tablename FROM pg_tables 
WHERE tablename = 'user_roles';

-- Проверить, что есть данные
SELECT * FROM user_roles LIMIT 10;

-- Проверить, что индекс создан
SELECT * FROM pg_indexes 
WHERE tablename = 'user_roles';
```

---

## 🔍 Диагностика проблем

### Чек-лист диагностики

```
□ Таблица user_roles создана в Supabase?
  → Суперbase Dashboard → Table Editor → есть ли user_roles?

□ SQL миграция выполнена без ошибок?
  → Supabase Dashboard → Logs → нет ошибок?

□ Бот запущен и работает?
  → Консоль: есть ли сообщений про бота?

□ Пользователь может выбрать роль?
  → Запустите /start в Telegram

□ Роль сохраняется в БД?
  → Supabase → Table Editor → user_roles → есть ли новая запись?

□ Роль находится при перезапуске?
  → Перезагрузите бот
  → Запустите /start в Telegram
  → Роль должна быть найдена автоматически

□ Роль попадает в contractors/orders?
  → Создайте анкету
  → Проверьте contractors/orders → есть ли role в новой записи?
```

---

## 🛠️ Команды для диагностики

### Проверить таблицу
```sql
-- Взглянуть на структуру таблицы
\d user_roles

-- Или через SQL:
SELECT column_name, data_type, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'user_roles';
```

### Проверить индексы
```sql
SELECT * FROM pg_indexes 
WHERE tablename = 'user_roles';
```

### Проверить триггеры
```sql
SELECT trigger_name, event_manipulation, event_object_table 
FROM information_schema.triggers 
WHERE event_object_table = 'user_roles';
```

### Проверить данные
```sql
-- Все роли в системе
SELECT role, COUNT(*) as count 
FROM user_roles 
GROUP BY role;

-- Последние добавленные роли
SELECT * FROM user_roles 
ORDER BY created_at DESC 
LIMIT 10;

-- Роль конкретного пользователя
SELECT role FROM user_roles 
WHERE telegram_id = YOUR_TELEGRAM_ID;
```

---

## 📝 Как сообщить об ошибке

**Информация, которая нужна:**
1. Точная текст ошибки (из консоли или Supabase Logs)
2. Какой шаг вы делали, когда произошла ошибка
3. Скриншот Supabase Table Editor (показывает структуру таблицы)
4. Версия кода (какую дату обновления bot.js)

**Где сообщить:**
- Комментарии в коде (если используете GitHub)
- Логи ошибок в Supabase Dashboard
- Сообщение в поддержку Supabase

---

## 💡 Часто задаваемые вопросы

### В: Можно ли восстановить старые роли из contractors/orders?

О: Да, используйте SQL миграцию в разделе "Обратная совместимость" в [USER_ROLES_IMPLEMENTATION.md](./USER_ROLES_IMPLEMENTATION.md)

### В: Как изменить роль пользователя?

О: Обновите запись в таблице `user_roles`:
```sql
UPDATE user_roles 
SET role = 'заказчик' 
WHERE telegram_id = YOUR_TELEGRAM_ID;
```

### В: Как добавить новую роль?

О: 
1. Обновите CHECK constraint в SQL
2. Добавьте новую кнопку в bot.js
3. Добавьте новое условие в обработчик выбора роли

### В: Как удалить пользователя из системы?

О: 
```sql
DELETE FROM user_roles WHERE telegram_id = YOUR_TELEGRAM_ID;
```

### В: Можно ли выбрать несколько ролей?

О: Нет, в текущей реализации один пользователь = одна роль (UNIQUE constraint).
Если нужно - свяжитесь с поддержкой.

### В: Как отследить историю изменений ролей?

О: Добавьте таблицу `user_role_history` (не реализовано, но легко добавить).
Или используйте поле `updated_at` для отслеживания последнего обновления.

---

## 🚀 Как получить помощь быстро

**Если у вас есть ошибка:**

1. **Скопируйте текст ошибки** (из консоли или Supabase Logs)
2. **Опишите, что вы делали** (нажал кнопку, заполнил форму, и т.д.)
3. **Проверьте логи**:
   - Логи бота в консоли
   - Supabase Logs в Dashboard
4. **Прочитайте соответствующий раздел в документации**
5. **Если всё равно не работает** - проверьте эту страницу (SUPPORT.md)

---

## 📚 Дополнительные ресурсы

- [Supabase документация](https://supabase.com/docs)
- [PostgreSQL документация](https://www.postgresql.org/docs/)
- [Node.js Telegram Bot API](https://github.com/yagop/node-telegram-bot-api)

---

## 🎯 Следующие шаги

После успешного внедрения вы можете:

1. **Добавить смену роли** - позволить пользователям менять роль
2. **Добавить аналитику** - статистика по распределению ролей
3. **Добавить историю** - отслеживание изменения ролей
4. **Добавить управление** - администраторские команды для изменения ролей

---

**Версия:** 1.0
**Дата:** 2025-12-24

**Удачи с внедрением! 🚀**
